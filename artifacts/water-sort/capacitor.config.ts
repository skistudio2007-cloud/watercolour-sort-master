import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.colourwatersort.puzzle',
  appName: 'Colour Water Sort: Puzzle Game',
  webDir: 'dist/public',
};

export default config;